.venv/
.cache/
__pycache__/
*.pyc
refresh.log
# Nothing in data/ belongs in the repo. The backtest CSVs come from
# vaastav/Fantasy-Premier-League and the Understat scrapes are re-fetchable; together
# they are ~77MB of third-party data that would otherwise be republished here.
data/
!data/README.md

# Prediction log — written by the tool, but it is a record, not a build artifact.
# Kept out of git so CI runs never overwrite the local one, and vice versa.
predictions.json
